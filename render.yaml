services:
  - type: worker
    name: ggod-fatherr-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PREMIUM_CODE
        sync: false
